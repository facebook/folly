When first reading this loader, load `{FA}/task-ledger-startup.md` once.

Load `{FA}/task-ledger.md` when:

- the user asks to `enqueue` work or mentions a task ledger; or
- the current request is not a single short, linear task.
