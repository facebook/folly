> NOT A RULE. If loaded as task policy, stop and ask the user.

Purpose: stop an agent from polishing the first plausible design before finding
missing requirements, checking the assumptions that could change the choice, and
rejecting options that cannot work.

Keep this as its own top-level rule because it governs choosing designs or
correctness fixes, not explaining them.
