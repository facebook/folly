Complete `request.md` against `adjacent_dedup.cpp`. Write the complete corrected
C++ source to `output.md`.

Use only the staged inputs and rules. Do not inspect parent directories, a live
checkout, source control, prior outputs, or network content. Write only
`output.md` and process artifacts required by the staged rules.
