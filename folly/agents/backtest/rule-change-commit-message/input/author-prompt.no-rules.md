Complete the request in `request.md`. The hydrated material it refers to is in
`requirements.md` and `conversation.md`. `commit-state.md` records the commit's
state when the request was made, and `change.patch` is its diff.

This reproduction cannot mutate source control. Write the complete replacement
commit message, including its title, Summary, and Test Plan, to `output.md`
instead, then stop after completing the work.

Use only the staged inputs. Write only `output.md`. Do not inspect parent
directories, a live checkout, source control, prior outputs, evaluator material,
or network content.
