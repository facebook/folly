Load `{FA}/rule-conflicts.md` after loading one or more other rule files, or
when a hook injects instructions. Stop if it is unavailable.

When editing `{FA}/`, also read `{FA}/README.md` and `{FA}/CONTRIB.md`.
