> NOT A RULE. If loaded as task policy, stop and ask the user.

Purpose: keep cross-language code changes easy to reason about without removing
structure that protects correctness or a real boundary.

- Loads focused testing guidance when tests are in scope.
- Language and project rules refine it separately.
