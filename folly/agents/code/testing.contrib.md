> NOT A RULE. If loaded as task policy, stop and ask the user.

Purpose: govern whether and how code changes need tests.

- Loaded through `code.md` when tests are in scope.
- Owns risk-based coverage, boundaries, mocking, refactoring, and pruning.
