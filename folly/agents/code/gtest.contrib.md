> NOT A RULE. If loaded as task policy, stop and ask the user.

Purpose: refine test rules for Google Test.

- Loaded through `testing.md` when Google Test is in scope.
- Owns Google Test-specific assertions and setup guidance.
