> NOT A RULE. If loaded as task policy, stop and ask the user.

# Code rules

`../code.md` owns cross-language code guidance. This directory holds focused
rules loaded only when their subject is in scope. `testing.md` covers general
test decisions; `gtest.md` adds Google Test-specific guidance.

Keep language and project policy separate unless it applies broadly enough to
belong in this portable package.
