> NOT A RULE. If loaded as task policy, stop and ask the user.

Purpose: make a source-aware reviewer form an independent standard before it
reads the candidate, then integrate the cold reader's report.

The task lists every file it may read. It decides what good output requires
before seeing the candidate, then checks the candidate. For prose, it also
integrates the cold reader's report. Orchestration and retry policy stay in
`../critic-iterate.md`.
